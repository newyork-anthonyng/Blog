<h1>My Projects</h1>

<ul>
  <% @project_posts.each do |post| %>
    <li><%= "#{post.title} | #{post.description}" %>
        | <a href="<%= post.project_url %>" target="_blank">Project Link</a> |
          <a href="<%= post.blog_url %>" target="_blank">Blog Link</a>
    </li>
  <% end %>
</ul>

<%= link_to "<< Back", "/" %>
